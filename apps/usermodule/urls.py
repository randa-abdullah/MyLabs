# Add URL patterns for usermodule here
# Example:
from django.urls import path
from . import views

urlpatterns = [
    # Define your URL patterns for usermodule here
    # Example:
    # path('somepath/', views.some_view, name='some_view'),
]
